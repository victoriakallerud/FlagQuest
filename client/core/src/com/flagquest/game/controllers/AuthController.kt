package com.flagquest.game.controllers
/*

import com.flagquest.game.models.FirebaseAuthInterface
import com.flagquest.game.states.LoginState

class AuthController(private val authModel: FirebaseAuthInterface, private val view: LoginState) {
    fun onSignInClicked(email: String?, password: String?) {
        authModel.signIn(email, password, object : AuthListener() {
            fun onSuccess() {
                view.showSignInSuccess()
            }

            fun onError(e: Exception) {
                view.showSignInError(e.message)
            }
        })
    } // Similar for signUp...
}
*/
